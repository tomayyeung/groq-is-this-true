{
  "name": "groq-is-this-true",
  "scripts": {
    "build": "esbuild background.js --bundle --outfile=dist/background.js --format=esm"
  },
  "dependencies": {
    "groq-sdk": "latest"
  },
  "devDependencies": {
    "esbuild": "^0.25.11"
  }
}
{
  "name": "groq-is-this-true",
  "scripts": {
    "build": "GROQ_API_KEY=$(grep GROQ_API_KEY .env | cut -d '=' -f2) esbuild background.js --bundle --outfile=dist/background.js --format=esm --define:process.env.GROQ_API_KEY='\"'$GROQ_API_KEY'\"'"
  },
  "dependencies": {
    "groq-sdk": "latest"
  },
  "devDependencies": {
    "esbuild": "^0.25.11"
  }
}
